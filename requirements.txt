# Local orchestration + analysis. The 70B models run on Modal (src/target_model.py,
# src/nla_modal.py), so torch/transformers/sglang are NOT needed locally — only in the
# Modal images. src/nla.py (vendored NLAClient) also runs only on the Modal worker.
modal>=0.64
huggingface_hub>=0.24
numpy>=1.24
python-dotenv>=1.0
